{% extends '::adminBase.html.twig' %}
{% set title = 'Tags' %}
{% set panel = 1 %}
{% block body -%}
    <table class="table table-striped table-bordered table-hover records_list" id="dataTables">
        <thead>
            <tr>
                <th style="width: 60px;">Id</th>
                <th>Name</th>
                <th style="width: 130px;">Datecreated</th>
                <th style="width: 130px;">Lastedited</th>
                <th style="width: 100px;">Enabled</th>
                <th style="width: 100px;">Deleted</th>
                <th style="width: 130px;">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tag_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{% if entity.dateCreated %}{{ entity.dateCreated|date('Y-m-d') }}{% endif %}</td>
                <td>{% if entity.lastEdited %}{{ entity.lastEdited|date('Y-m-d') }}{% endif %}</td>
                <td>{{ entity.enabled }}</td>
                <td>{{ entity.deleted }}</td>
                <td>
                    <a href="{{ path('tag_show', { 'id': entity.id }) }}" type="button" class="btn btn-default">show</a>
                    <a href="{{ path('tag_edit', { 'id': entity.id }) }}" type="button" class="btn btn-default">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <ul>
        <li>
            <a href="{{ path('tag_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
